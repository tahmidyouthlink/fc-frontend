/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ibb.co",
      "i.ibb.co.com",
      "storage.googleapis.com",
      "media.poshax.shop",
    ],
  },
  reactStrictMode: false,
};

export default nextConfig;
